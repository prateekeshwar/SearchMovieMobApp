import React from 'react';
import {SafeAreaView} from 'react-native';
import MovieDescription from './src/Screen/MovieDescription';

function App(): JSX.Element {
  return (
    <SafeAreaView style={{}}>
      <MovieDescription />
    </SafeAreaView>
  );
}

export default App;
